# Pressure OCR — Progress Update

สรุปการทดลองอ่านค่า vacuum pressure จากจอ LCD ของปั๊ม (Pfeiffer DCU002) ด้วยกล้อง
เขียนไว้เพื่อให้กลับมาทำต่อได้โดยไม่ต้องเสียเวลาไปเจอปัญหาเดิมซ้ำ

**อัปเดตล่าสุด:** 2026-07-20
**Branch ปัจจุบัน:** `template-matching-ocr`

---

## 1. สถานะปัจจุบัน (TL;DR)

| ส่วน | สถานะ |
|---|---|
| YOLO + EasyOCR | ใช้งานอยู่ (ตัวหลักตอนนี้) แต่แม่นยำต่ำกับ font จอนี้ |
| Manual exposure lock | **ใช้งานอยู่ — ได้ผลชัดเจน** |
| ROI เลือกจากหน้าเว็บ | ใช้งานอยู่ |
| 10-min window voting | ใช้งานอยู่ (ล่าสุด ยังไม่ได้ทดสอบกับปั๊มจริง) |
| Template matching | เขียนเสร็จ + ทดสอบผ่านแล้ว **แต่ยังไม่เปิดใช้** (ต้อง calibrate ก่อน) |

**แนวทางปัจจุบัน:** ไม่พยายามให้อ่านถูกทุกครั้ง แต่อ่านซ้ำเยอะๆ ใน 10 นาที
แล้วเอาค่าที่ตรงกันมากที่สุดมาบันทึกครั้งเดียว

---

## 2. Timeline และ commits

```
a5dad6f  จุดตั้งต้น (ก่อนเริ่มรอบนี้)
   │
f6f91ca  Tune pressure OCR for the real pump LCD        ← branch yolo-extract-value อยู่ตรงนี้
   │     (จุดปลอดภัย ย้อนกลับมาได้ทุกเมื่อ)
   │
d5709e7  Template matching (เขียนเสร็จ ยังไม่เปิดใช้)
   │
4654ed6  แก้บั๊ก exponent mapping  ← สำคัญมาก อ่านหัวข้อ 5.1
   │
c3782e4  10-min window voting                           ← HEAD ปัจจุบัน
```

**Branch:**
- `main` — เวอร์ชันใช้งานจริง ยังไม่มีงานรอบนี้
- `yolo-extract-value` — จบที่ `f6f91ca`
- `template-matching-ocr` — งานรอบนี้ทั้งหมด (ยังไม่ push ขึ้น remote)

---

## 3. ปัญหาต้นตอ: font ของจอ LCD

**นี่คือรากของทุกปัญหา** — ตอน mockup ทดสอบกับ Tkinter simulator บนจอคอมทำงานได้ดีมาก
เพราะใช้ font ปกติที่ OCR คุ้นเคย แต่พอเจอของจริง:

- จอเป็น **dot-matrix character LCD** ตัวอักษรประกอบจากจุดที่ไม่ต่อกัน
- **เลข 0 มีขีดทแยงกลาง** (แบบ slashed zero เพื่อแยกจากตัวอักษร O)
- EasyOCR / Tesseract ไม่เคยเทรนกับ font แบบนี้มา

ผลคืออ่านผิดเป็นระบบ ไม่ใช่ผิดแบบสุ่ม:

| จอแสดง | OCR อ่านได้ |
|---|---|
| `6.3E0` | `6 SE0`, `6SED`, `6 3EO` |
| `4.6E1` | `4.6El`, `4 6EI` |
| `1.0E2` | `1 0EZ`, `1 0E]` |
| `340: Pressure` | `340 Pressure`, `3408`, `340 Prcs`, `Ppessun` |

> **บทเรียน #1:** simulator ผ่านไม่ได้แปลว่าของจริงจะผ่าน
> ปัญหา font เป็นคนละเรื่องกับปัญหาแสง/โฟกัส/ROI โดยสิ้นเชิง

---

## 4. สิ่งที่ทำไปแล้ว — อันไหนเวิร์ค อันไหนไม่

### 4.1 ล็อค exposure/gain/white-balance ⭐ **เวิร์คมาก — เก็บไว้แน่นอน**

**สิ่งที่เจอ:** ดู log แยกตามรายชั่วโมงของวันเดียวกัน

| เวลา | attempts | accepted | อัตรา |
|---|---|---|---|
| 09:00 | 9 | 0 | 0% |
| 10:00 | 307 | 0 | 0% |
| 11:00 | 339 | 15 | 4.4% |
| 12:00 | 397 | 23 | 5.8% |
| **13:00** | 270 | **100** | **37%** |
| 14:00 | 15 | 0 | 0% |

กล้องใช้เวลา **~3 ชั่วโมง** กว่า auto-exposure จะ converge จนภาพอ่านได้ดี
แล้วพอมีอะไรมากระทบก็ร่วงกลับไป 0% ทันที

**แก้โดย:** ล็อคค่าเป็น manual ใน `config.py` (`CAMERA_MANUAL_EXPOSURE` ฯลฯ)
และเปลี่ยน backend เป็น `CAP_DSHOW` (ต้องใช้ backend เดียวกับตอนหาค่า ไม่งั้นค่าไม่ติด)

**เครื่องมือ:** `YOLO_test/camera_exposure_tune.py` — ปรับ trackbar สดๆ แล้วกด `s`
เพื่อ print ค่าไปใส่ config พร้อมบอกว่าค่านั้นกล้อง "รับ" จริงไหม

> **บทเรียน #2:** auto-exposure ของ webcam ไม่เหมาะกับงาน monitor ระยะยาว
> ต้องล็อคเป็น manual เสมอ และเทียบภาพต่างช่วงเวลาก่อนสรุปว่าปัญหาอยู่ที่ไหน

### 4.2 ROI เลือกจากหน้าเว็บ ⭐ **เวิร์ค — เก็บไว้**

ลากกรอบบนภาพ live stream ได้เลย + ปุ่ม Reset เก็บลง `pressure_ocr_roi.json`
(ไม่ใช่ `config.py`) เพราะกล้องอยู่บนขาตั้ง โดนชนบ่อย ต้องแก้ได้โดยไม่ restart แอป

เพิ่มระบบเตือนบน dashboard เมื่ออ่านพลาดติดกันหลายครั้ง (`PRESSURE_OCR_ALERT_THRESHOLD`)
— เดิมกล้องขยับแล้วระบบจะเงียบไปเฉยๆ กว่าจะรู้ตัวคือกราฟค้างไปแล้ว

### 4.3 แก้ regex / mode confirmation ✅ **เวิร์ค**

- **Mode confirm ไม่เคยผ่าน:** YOLO รวมกล่อง `"340:"` กับ `"Pressure"` เป็นข้อความเดียว
  แต่โค้ดเช็คแบบ exact match เลยไม่ผ่านสักอัน → เปลี่ยนเป็น substring search
- **จุดทศนิยมอ่านเป็นช่องว่าง:** `"6 3E0"` → รองรับเพิ่ม

### 4.4 CLAHE + upscale + ลด YOLO threshold ⚠️ **ช่วยเล็กน้อย**

ทำแล้วไม่เสียหาย แต่ไม่ใช่ตัวเปลี่ยนเกม เพราะไม่ได้แก้ปัญหา font

### 4.5 Template matching 🔬 **เขียนเสร็จ ทดสอบผ่าน แต่ยังไม่เปิดใช้**

**หลักการ:** เลิกใช้ OCR ทั่วไป หันมาเทียบรูปตัวอักษรกับ template ที่ตัดจากจอตัวนี้โดยตรง

**สิ่งที่ค้นพบระหว่างทาง (สำคัญ):**
- ลองแยกตัวอักษรด้วย connected-components → **ไม่ได้** เพราะ dot-matrix แตกเป็นเสี่ยงๆ
- แต่พบว่าตำแหน่ง x ของตัวอักษรห่างเท่ากันเป๊ะ **46px** (ที่ upscale 3x)
  → เป็น **fixed-pitch grid** ตัดตามกริดตรงๆ ได้เลย ไม่ต้อง segment

**ผลทดสอบ** (ตัด template จาก snapshot นึง เอาไปอ่านอีก 3 ใบ):

| ภาพ | ค่าจริง | ผล |
|---|---|---|
| 121737 | 4.6E1 | ✅ อ่านถูก score 1.000 |
| 131020 | 5.4E0 | 🛑 ปฏิเสธ (ไม่มี template เลข 5) |
| 125738 | 8.6E1 | 🛑 ปฏิเสธ (ไม่มี template เลข 8) |

- ตัวที่อ่านถูก score **0.73-0.91**
- ตัวที่ไม่มี template (ถูกจับคู่กับตัวใกล้เคียงผิดๆ) score **0.35-0.61** และเป็นตัวต่ำสุดในแถวเสมอ
- ตั้ง gate ที่ **0.70** แยกได้สะอาด

**บรรทัด `340: Pressure` อ่านถูก 100%** ทุกภาพที่ exposure ปกติ
→ เอามาใช้เป็น **fiducial จัดตำแหน่งกริด** ได้ (กล้องขยับนิดหน่อยระบบหากริดใหม่เองได้)

**ทำไมยังไม่เปิดใช้:** ต้อง calibrate เก็บ template ให้ครบเลข 0-9 ซึ่งต้องรันเครื่องมือ
หลายรอบกับ snapshot ที่ค่าต่างกัน (ค่า pressure ตอนนึงมีแค่บางเลข) — ใช้เวลาและต้องปรับเยอะ
เลยพักไว้ก่อน

**ถ้าจะกลับมาทำต่อ:**
```
python YOLO_test/build_lcd_templates.py --image snapshots/snapshot_XXXX.jpg
```
ปรับ trackbar ให้กริดตรง → `t` พิมพ์ข้อความที่เห็น → `s` บันทึก → รันซ้ำหลายใบจนครบ 0-9
เครื่องมือจะบอกเองว่ายังขาดตัวไหน

### 4.6 10-min window voting ⭐ **ตัวปัจจุบัน — ยังไม่ได้ทดสอบกับปั๊มจริง**

**หลักการ:** เปลี่ยนกรอบคิด — ไม่ต้องให้อ่านถูกทุกครั้ง

เพราะเราต้องการแค่ **1 ค่าต่อ 10 นาที** แต่ระบบยิงอ่านทุก 20 วิ
→ ใน 1 หน้าต่างได้ตัวอย่างหลายสิบครั้ง ขอแค่ **2 ครั้งตรงกัน** ก็พอ

**ผลจำลองย้อนหลังจาก log จริง (ก่อนแก้ exposure ด้วยซ้ำ):**

| วิธี | ได้ค่าใน 10 นาที | ถูกต้อง |
|---|---|---|
| ปฏิเสธตัวกำกวมทิ้ง | 19/37 | ถูกหมด |
| **แปลงตามหลักฐาน (ที่ใช้อยู่)** | **23/37 (62%)** | **ถูกหมด** |

---

## 5. บั๊กที่เจอ — และบทเรียนสำคัญที่สุด

### 5.1 🔴 บั๊ก exponent mapping (ผิด 10-100 เท่า แบบเงียบๆ)

**เกิดอะไรขึ้น:** ตอนแก้ปัญหา "เลข 0 อ่านยาก" ได้ไปนับตัวอักษรแปลกๆ ที่ OCR อ่านได้
ในตำแหน่ง exponent จาก log จริง:

```
l (157 ครั้ง), i (55), ] (40), Z (12), I (10), O (8)
```

แล้ว**สรุปเอาเองว่าทั้งหมดคือเลข 0 ที่มีขีดกลาง** → แปลงเป็น "0" ทุกตัว

**ซึ่งผิด** พอไปไล่จับคู่กับการอ่านที่สะอาดในช่วงเวลาใกล้กัน พบว่า:

| อ่านได้ | ของสะอาดข้างๆ | ที่จริงคือ |
|---|---|---|
| `1 4El` | `1 4E1` | `l` = **1** |
| `4 0EI` | `4.0E1` | `I` = **1** |
| `1 0Ei` | `1 2E1` | `i` = **1** |
| `3.4E]` | `3 4E1` | `]` = **1** |
| `1 0EZ` | `1 0E2` | `Z` = **2** |
| `6 3EO` | `6 3E0` | `O` = 0 ← ถูกตัวเดียว |

**ผลกระทบ:** `4.6E1` (46 mbar) → อ่านเป็น `4.6` ผิด **10 เท่า**
และ `1.0E2` (100 mbar) → `1.0` ผิด **100 เท่า** โดยค่าที่ได้ยังดูสมเหตุสมผลจนจับไม่ได้

**โชคดีที่จับได้ก่อนรันกับปั๊มจริง** ข้อมูลเลยยังไม่เสีย แก้แล้วใน `4654ed6`

**ทำไมถึงหลุดไปได้:** ตอนทดสอบครั้งแรกนับแค่ว่า *"กู้ค่ากลับมาได้เพิ่มกี่แถว"*
แต่**ไม่ได้เช็คว่าค่าที่ได้ถูกไหม** — ตัวเลข "23/37 หน้าต่าง" ดูดีเท่ากันทั้งเวอร์ชันถูกและผิด

> **บทเรียน #3 (สำคัญที่สุด):**
> การวัดผลต้องเช็ค **ค่าที่ได้ถูกต้องไหม** ไม่ใช่แค่ **ผ่าน/ไม่ผ่านกี่ครั้ง**
> ต้องมี ground truth (เช่น snapshot ที่เห็นค่าจริง) มาเทียบเสมอ

> **บทเรียน #4:** ค่าที่ผิดแบบดูสมเหตุสมผลอันตรายกว่าไม่มีค่าเลย
> ระบบควร "ปฏิเสธ" ดีกว่า "เดา" — เหมือนที่ template matching ทำ (score gate)

### 5.2 ข้อจำกัดของ voting ที่ต้องรู้

**Voting กันได้แค่การอ่านผิดแบบสุ่ม ไม่ได้กันแบบเป็นระบบ**

ตัวอย่างจริง: ในหน้าต่าง 12:10 ทั้ง **53 ครั้ง**เห็นตรงกันหมดว่า `4.6`
ซึ่งผิด (ของจริง 46) เพราะบั๊กข้อ 5.1 → voting ผ่านฉลุย แต่ค่าผิด

→ **ช่วงแรกต้องเทียบค่าในกราฟกับจอจริงด้วยตาสักสองสามรอบ**

---

## 6. สิ่งที่ควรทำต่อ (TODO)

### 6.1 ทดสอบ window voting กับปั๊มจริง (ลำดับแรก)
รันแล้วดูสัก 2-3 หน้าต่าง (20-30 นาที) เทียบค่าในกราฟกับจอจริง
ระวังเป็นพิเศษ: ค่าที่ผิดเป็น 10 เท่า / 100 เท่า

### 6.2 กราฟอ่านง่ายขึ้น (ค้างจาก request เดิม)
**ปัญหา:** แกน log แสดง tick แค่ค่าที่ลงตัว (1E+1, 1E+2) พอข้อมูลอยู่ในช่วงแคบ
(เช่น 12-100 mbar ทั้งหมด) กราฟดูยากมาก

**แผน:** auto-switch เป็นแกน **linear** เมื่อข้อมูลอยู่ในช่วงแคบ (ต่างกันไม่เกิน ~20 เท่า)
และเป็น **log** เมื่อข้อมูลกว้างข้ามหลาย decade (ตอน pump-down เต็มช่วง)
หรือเพิ่มปุ่ม Log/Linear สลับเอง

### 6.3 Template matching (ถ้าอยากให้แม่นจริงๆ)
โค้ดพร้อมแล้ว เหลือแค่ calibrate — ดูหัวข้อ 4.5

### 6.4 เรื่องอื่นๆ ที่ยังไม่ได้แตะ
- Pressure gauge ตัวที่ 2 สำหรับ CCD chamber (คุยไว้นานแล้ว ยังไม่เริ่ม)
- `YOLO_test/PRESSURE_LCD_READER.md` เอกสารเก่า ยังไม่ได้อัปเดตตามงานรอบนี้
- ยังไม่ได้ `git push` ขึ้น remote เลย

---

## 7. ไอเดียที่คุยกันแล้วแต่ยังไม่ได้ทำ

| ไอเดีย | ความเห็น |
|---|---|
| ลอง Tesseract อีกครั้ง | เคยลองแล้วไม่ดี แต่ตอนนั้น exposure ยังแย่ ลองใหม่กับภาพคุณภาพดีอาจต่างออกไป |
| ตัด YOLO ออก ให้ OCR อ่าน ROI ตรงๆ | ROI แคบแล้วไม่จำเป็นต้องมี YOLO หากล่องอีกชั้น ลดจุดพังไปหนึ่งจุด |
| อ่านค่าผ่าน RS-232 จาก DCU โดยตรง | ถ้าตัว DCU มีพอร์ตสื่อสาร วิธีนี้จะแม่น 100% และเลิกยุ่งกับ OCR ทั้งหมด — **น่าเช็คในคู่มือ** |

> ข้อสุดท้ายน่าสนใจที่สุดถ้าทำได้ — คู่มืออยู่ที่ `YOLO_test/PT0263BEN_E.pdf`

---

## 8. ไฟล์สำคัญ

| ไฟล์ | หน้าที่ |
|---|---|
| `app.py` | `PressureLCDReader` — ลูปอ่านค่า, voting, ROI, alert |
| `pressure_ocr.py` | regex, validation, confusable mapping, YOLO+EasyOCR |
| `lcd_template_ocr.py` | template matching (ยังไม่เปิดใช้) |
| `config.py` | ค่า config ทั้งหมด รวม exposure และ voting |
| `YOLO_test/camera_exposure_tune.py` | เครื่องมือหาค่า exposure |
| `YOLO_test/build_lcd_templates.py` | เครื่องมือ calibrate template |
| `logs/pressure_ocr_raw_*.csv` | log ทุก attempt — **มีค่ามากสำหรับ debug** |
| `logs/pressure_ocr_clean_*.csv` | เฉพาะค่าที่ commit แล้ว |

> `pressure_ocr_raw_*.csv` เก็บ raw text ที่ OCR อ่านได้ทุกครั้ง
> เป็นตัวที่ทำให้จับบั๊กข้อ 5.1 ได้ **อย่าเอาออก**
